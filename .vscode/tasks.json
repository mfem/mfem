{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build mfem ex99",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build", "${workspaceFolder}/build",
                "--target", "ex99_test_PML",
                "--config", "Debug"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
